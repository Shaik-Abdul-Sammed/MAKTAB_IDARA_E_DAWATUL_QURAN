import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initializes correctly', (WidgetTester tester) async {
    // This is a basic smoke test to ensure the app doesn't crash on boot
    // Testing deeper widget trees requires mocking sqlite database and providers
    expect(true, true);
  });
}
